#!/bin/bash

type=$1
location="sanandaj"

if [ "$type" == "stat" ];then
  curl "v2d.wttr.in/${location}"
elif [ "$type" == "line" ]; then
  curl "wttr.in/${location}?format=3"
else
  curl "wttr.in/${location}"
fi
